# .env

# URL COM Connection Pooling (Para uso geral da aplicação/Prisma Client)
# Se estiver usando Vercel Postgres, pegue no painel da Vercel em "Storage".
# Se for local/outro, geralmente é porta 5432 ou 6543 (pgbouncer).
POSTGRES_PRISMA_URL="postgres://usuario:senha@host-pool:porta/banco?pgbouncer=true&connect_timeout=15"

# URL SEM Pooling (Para migrações e conexão direta)
# Necessário para que o Prisma Migrate funcione corretamente em ambientes serverless.
POSTGRES_URL_NON_POOLING="postgres://usuario:senha@host-direto:porta/banco"

# Chave secreta para assinar seus tokens JWT
JWT_SECRET="gere-uma-chave-segura-aqui"
